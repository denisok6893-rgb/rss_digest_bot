# CONTINUITY — rss_digest_bot

## Что это
Telegram-бот для работы с RSS-лентами.

Поддерживаемые команды:
- /add /list /del — управление RSS-каналами
- /sync — ручная синхронизация (скачивание новых записей в SQLite)
- /news — новости за сегодня
- /news week — новости за последние 7 дней

---

## Как запускать (важно)
Запускать только через:
  ./run.sh

Если бот “молчит” — первое проверить:
- что он запущен именно через ./run.sh
- что systemd-сервис активен

---

## Хранилище
- SQLite: data/rss.db (не коммитится)
- .env с BOT_TOKEN (не коммитится)

---

## Установка
- Python + venv
- requirements.txt фиксирует зависимости
- Деплой на сервер: /opt/rss_digest_bot
- Запуск через systemd, режим polling

---

## Контекст проекта
Проект rss_digest_bot.
Бот разворачивается на сервере (/opt/rss_digest_bot) и работает через systemd в режиме polling.
Раньше изменения частично делались локально (Termux), из-за чего возникала рассинхронизация.
Сейчас сервер — основной источник истины.

---

## Что сделано

### UI и навигация
Добавлено главное меню с Reply-кнопками:
- 📰 Сегодня
- 📅 Неделя
- ➕ Добавить RSS
- 📃 Мои источники
- ⚙️ Настройки

Вынесена клавиатура в отдельный модуль:
- app/keyboards/main_menu.py
- добавлены __init__.py в app/ и app/keyboards/

/start теперь:
- отвечает текстом
- показывает клавиатуру через reply_markup=main_menu_keyboard()

Реализован handle_menu_buttons:
- кнопки вызывают существующие команды
- без изменения update.message.text (исправлен AttributeError)
- Авто-sync включён и проверен: timer active, service SUCCESS, лог sync.log пишется.
- Добавлена защита от “битых” RSS: в subscriptions поля fail_count, disabled, last_error.
- CLI-sync пропускает disabled=1, увеличивает fail_count при ошибках, сбрасывает при успехе.
- При fail_count >= SYNC_MAX_FAILS (по умолчанию 5) подписка авто-отключается и отправляется Telegram-уведомление.
- Ошибки sync возвращают exit-code=1 для systemd (алерты/FAILED).

### Авто-sync по расписанию

- Настроен авто-sync через systemd timer  
  Сервис: `rss_digest_bot_sync.service`  
  Таймер: `rss_digest_bot_sync.timer` (каждые 30 минут)

- Добавлен CLI-режим синхронизации:

---

### Деплой и стабильность запуска
Исправлены ошибки инициализации:
- application.add_handler → app.add_handler
- восстановлен импорт ContextTypes

Критичный фикс деплоя:
- systemd запускает бот как модуль:
  ExecStart=/opt/rss_digest_bot/.venv/bin/python -m app.main
- задан WorkingDirectory=/opt/rss_digest_bot
- устранена ошибка ModuleNotFoundError: No module named 'app'

---

### Логика новостей (исправлено)
Проблема: “Сегодня” и “Неделя” выдавали одинаковое.
Причина: кнопка “Неделя” не передавала аргумент week, плюс были проблемы с фильтрацией.

Сделано:
- кнопка “Неделя” передаёт context.args = ["week"]
- исправлен search_news_advanced:
  - корректный фильтр за последние 7 дней
  - поддержка query и category
  - сортировка по published_at DESC
- get_today_news переведён на localtime:
  - date(published_at, 'localtime') = date('now', 'localtime')

Результат:
- “Сегодня” и “Неделя” различаются
- /news week работает стабильно

---

### Синхронизация и UX
- /sync остаётся командой с сообщением о результате
- для кнопок убрано лишнее сообщение “Синхронизация готова” (тихий сценарий для выдачи)

---

### Репозиторий
- изменения закоммичены и запушены
- добавлен app/scheduler.py (заготовка под авто-sync)
- обновлён requirements.txt (зависимости)

---

### Логирование авто-sync (CLI sync)

- Добавлено логирование CLI-синхронизации в файл:
  - /opt/rss_digest_bot/logs/sync.log
- Формат строк:
  - [YYYY-MM-DD HH:MM:SS] start users=<N> feeds=<N>
  - [YYYY-MM-DD HH:MM:SS] done new_entries=<N> errors=<N> duration_sec=<S>
- Проверка:
  - /opt/rss_digest_bot/.venv/bin/python -m app.main sync
  - tail -n 20 /opt/rss_digest_bot/logs/sync.log

### Ротация логов auto-sync

- Настроена ротация логов через `logrotate`
- Конфигурация:
  - файл: `/etc/logrotate.d/rss_digest_bot`
  - лог: `/opt/rss_digest_bot/logs/sync.log`
- Параметры:
  - ротация: ежедневно
  - хранение: 14 дней
  - сжатие старых логов (`compress`, `delaycompress`)
  - безопасная обрезка активного файла (`copytruncate`)
- Проверка:
  - `logrotate -f /etc/logrotate.d/rss_digest_bot`
  - `ls -l /opt/rss_digest_bot/logs`

### Авто-alert через systemd (ошибки sync)

- CLI-sync теперь завершает процесс с кодом != 0 при наличии ошибок (failed > 0)
- Это позволяет systemd помечать запуск rss_digest_bot_sync.service как FAILED
- Исправлено: UnboundLocalError для переменной failed (инициализация счётчиков в начале run_sync_cli)
- Проверка:
  - /opt/rss_digest_bot/.venv/bin/python -m app.main sync
  - echo $?
  - journalctl -u rss_digest_bot_sync.service -n 80 --no-pager
- Исправлено: run_sync_cli() теперь завершает процесс с кодом 1, если errors > 0 (после выполнения цикла sync).
- Проверка: добавляли тестовую подписку на http://127.0.0.1:9/bad.xml → Sync done... Errors: 1, exit-code: 1, запись в logs/sync.log с errors=1.
- Эффект: systemd помечает rss_digest_bot_sync.service как FAILED при ошибках синхронизации, таймер остаётся включён.

## Текущее состояние
- бот работает
- кнопки отображаются и обрабатываются
- новости приходят корректно
- “Сегодня” и “Неделя” отличаются
- сервис запускается через systemd без ModuleNotFoundError
- Авто-sync + авто-alert работают: таймер активен, при ошибках sync сервис получает non-zero exit code.

---

## Известные ограничения / долги
- ⚙️ Настройки пока заглушка
- нет /news month
- фильтрация по теме/категории есть в cmd_news, но нужно дополировать UX и лимиты
- авто-sync по расписанию ещё не включён (есть заготовка scheduler)

---

##python -m app.main sync
Выполняет sync и **выходит**, без запуска polling

- Устранён конфликт Telegram:

##Conflict: terminated by other getUpdates request
Причина была в запуске второго polling-процесса таймером  
Решение: таймер использует CLI-sync, polling запускается только основным сервисом

## Следующий шаг (один)
Сделать авто-sync по расписанию:
- предпочтительно через systemd timer
- альтернативно через app/scheduler.py (если решим запускать внутри процесса)
